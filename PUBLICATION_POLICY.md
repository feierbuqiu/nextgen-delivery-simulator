# 公开发布政策

本仓库采用“最小公开树”原则：只接收运行、理解、测试和维护当前本地应用所必需的文件。私有研发仓库的历史、审计材料、研究笔记、验收证据、二进制视觉基线和部署记录不属于公开成果。

## 每次提交前

1. 只在该独立仓库中修改公开文件，不从私有仓库整目录复制。
2. 新增数据前阅读 `DATA_POLICY.md`，确认内容完全虚构或合成。
3. 依赖变化后运行 `npm run generate:metadata`。
4. 所有文件稳定后运行 `npm run generate:manifest`。
5. 暂存拟提交文件，再运行 `npm run check:staged`。

`npm run setup` 会启用随仓库提供的本地钩子。提交钩子检查实际 Git 索引，而不只检查未暂存工作树；推送钩子会运行完整发布门禁和依赖安全审计。钩子可以被本机用户绕过，因此上游仓库仍以必需的远端检查和分支规则作为最终合并边界。

## 合并与发布

- `main` 只接受拉取请求，不接受日常直接推送或未经检查的批量同步。
- 所有必需状态检查必须针对拉取请求最新提交成功，且分支必须与最新 `main` 保持同步。
- 自动化工作流使用只读默认权限、固定完整提交哈希的官方 Action，不持有部署密钥，也不执行高权限环境中的 fork 代码。
- 禁止强制推送、删除受保护分支和绕过必需检查；治理配置本身由默认分支上的可信工作流复核。
- 任何来源或隐私疑点都应阻止合并，直到相关材料被移除或问题得到解决。

本门禁降低误提交风险，但不能替代必要的人工判断或适用法域的专业法律意见。
