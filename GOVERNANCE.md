# 仓库治理

## 约束范围

上游仓库可以决定哪些变更进入受保护的 `main`，但不能接管第三方 fork 的管理员权限。公开 fork 不会自动继承上游的分支规则；任何人都可以在自己的 fork 中删除文件或关闭检查。上游通过可信治理守卫、必需质量检查和受保护分支确保：不符合规范的 fork 变更不能合并回来。

## 普通贡献路径

1. 贡献者 fork 仓库并创建主题分支。
2. 贡献者按需显式运行暂存快照、发布级检查和依赖审计；仓库不安装本地 Git 钩子，不在本机拦截提交或推送。
3. 拉取请求触发公开内容、架构、覆盖率、构建、本机边界、Windows 兼容和依赖变更检查。
4. 默认分支上的可信治理守卫通过接口读取变更清单，不签出、不安装、也不执行 fork 代码。
5. 所有必需状态针对最新提交成功且分支保持最新后，才允许合并到 `main`。合并方式依次选择 merge commit、squash、rebase；前一种方式可用时不得改用后一种方式。

普通拉取请求所需 reviewer 数量固定为 0，不要求人工批准，但机器门禁不可跳过。维护者仍可基于安全、隐私、项目范围、质量或法律风险拒绝合并。

## 受保护治理内容

项目宪章、工作流、本地钩子、检查器、编译与测试配置、本机运行入口及安全政策属于受保护内容。普通拉取请求修改这些路径时，治理守卫直接失败。

确有必要修正治理实现时，维护者必须：

1. 使用单独的拉取请求；
2. 添加“治理变更”标签；
3. 在说明中加入“治理变更说明”，列出原因、影响、回退方法和未被放宽的永久边界；
4. 保持所有既有检查成功，并增加能够证明新约束的测试或静态检查。

## 历史与供应链

- `package-lock.json` 固定完整依赖树及完整性哈希。
- `THIRD_PARTY_NOTICES.md` 与 `SBOM.spdx.json`记录每个锁定依赖及其声明许可证。
- `PUBLICATION-MANIFEST.json`记录公开树中每个文件的字节数和 SHA-256。
- GitHub Action 只允许官方仓库并固定到完整提交哈希；仓库已配置 Dependabot 每周检查 npm 与 GitHub Actions 依赖并提出升级拉取请求。
- 上游功能分支允许未签名提交，以便贡献者和自动化 Agent 正常协作；进入 `main` 的最终提交仍须由 GitHub 验证签名，并由远端规则执行。
- `main` 只接受拉取请求合并，不接受直接提交；合并方式优先使用 merge commit，其次 squash，最后才是 rebase。
- 受保护的 `main` 和发布标签禁止强制更新或删除；发布产物如果将来产生，应另行生成可验证的构建证明。

远端规则的首次启用与验收步骤见 `REPOSITORY_SETUP.md`。仓库内文件不能替代 GitHub 设置，二者必须同时成立。
