# 贡献指南

提交贡献即表示该贡献可以按 MIT 许可证公开。

## 开始开发

1. 阅读 `PROJECT_CHARTER.md`、`DATA_POLICY.md` 与 `PUBLICATION_POLICY.md`。
2. 从上游仓库 fork 后创建独立分支，不直接以 fork 的默认分支作为长期开发分支。
3. 运行 `npm ci`。仓库不安装本地 Git 钩子，也不拦截贡献者在本机提交或推送；质量要求由显式检查命令和远端 CI 执行。
4. 不提交真实个人数据、现实组织的非公开内容、来源不明素材、秘密信息或外部部署配置。

## 分支与提交

- 分支建议使用 `feature/`、`fix/`、`docs/`、`test/`、`refactor/`、`security/` 或 `governance/` 前缀。
- 提交标题采用“类型：中文简述”，类型可用“功能、修复、文档、测试、重构、维护、安全、治理”。
- 每个提交只处理一个可说明、可验证、可回退的主题。
- 功能分支提交不要求签名；进入上游 `main` 的最终提交必须带有可由 GitHub 验证的密码学签名。该要求由远端 `main` 规则执行，仓库不在贡献者本机拦截。
- 禁止直接向上游 `main` 提交或强制推送；所有后续变更必须通过拉取请求进入。
- 用户界面、说明文档、议题和拉取请求说明以简体中文为默认语言；代码标识符及必要技术名词不受此限制。

## 提交拉取请求前

1. 添加或更新与行为变化相匹配的测试。
2. 依赖变化后运行 `npm run generate:metadata`。
3. 所有文件稳定后运行 `npm run generate:manifest`。
4. 运行 `npm run check:release` 与 `npm audit --audit-level=high`。
5. 暂存拟提交快照后运行 `npm run check:staged`。
6. 按中文拉取请求模板说明影响、测试和公开边界。

拉取请求通过全部机器门禁后，合并方式依次选择 merge commit、squash、rebase；前一种方式可用时不得改用后一种方式。普通拉取请求所需 reviewer 数量为 0。

普通贡献不得修改项目宪章、自动化工作流、质量门禁、本机运行边界或安全配置。确需修正治理实现时，必须使用单独的拉取请求，由维护者添加“治理变更”标签并填写治理变更说明；任何放宽项目永久边界的修改均不接受。
