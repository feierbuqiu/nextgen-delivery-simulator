# 新仓库启用清单

本文件记录首次提交之后必须在 GitHub 完成的设置。文件存在本身不会自动启用远端保护。

## 首次引导

1. 创建名为 `nextgen-delivery-simulator` 的空公开仓库，不自动生成说明、许可证或忽略文件。
2. 只将通过 `npm run check:release` 与 `npm run check:staged` 的本候选树作为首次提交推送到 `main`。
3. 首次提交后立即完成下列安全与规则设置；在真实拉取请求验收完成前，不接受外部贡献。
4. 创建“治理变更”标签，只允许维护者把它用于独立的治理拉取请求。

## Actions 设置

- 只允许运行 GitHub 官方 Action，并启用“要求 Action 固定到完整提交 SHA”。
- 默认工作流权限设为只读，不允许 Actions 创建或批准拉取请求。
- fork 工作流选择“所有外部贡献者均需批准后运行”，审批前先查看文件变更。
- 只使用标准 GitHub 托管运行器，不配置自托管或大型运行器，不保存缓存和构建产物。

## 安全功能

启用依赖关系图、依赖漏洞提醒、依赖安全更新、秘密扫描、推送保护、私密漏洞报告和 CodeQL 默认设置。代码扫描应覆盖 JavaScript/TypeScript 与 GitHub Actions，并使用扩展查询套件。高等级未解决告警应阻止合并。

## `main` 规则集

建立无绕过名单的活动规则集，并至少启用：

- 合并前必须有拉取请求；
- 合并前分支必须与最新 `main` 同步；
- 必须解决全部对话；
- 普通拉取请求所需 reviewer 数量为 0，不要求人工批准；
- 允许 merge commit、squash 和 rebase，并按此顺序选择；前一种方式可用时不得改用后一种方式；
- 禁止强制推送、删除和重命名；
- 必须通过由 GitHub Actions 应用产生的“公开内容与质量门禁”“Windows 本机兼容检查”“依赖变更审查”和提交状态“治理规则未被绕过”；
- 必须通过代码扫描结果，阻止错误级以及高等级以上安全告警。

## 所有分支签名规则集

建立覆盖仓库全部分支、无绕过名单的活动规则集，要求每个提交都带有 GitHub 验证通过的密码学签名。该规则用于确认提交完整性，不作为额外权属声明。第三方 fork 不受上游管理员设置直接控制，但未签名或验证失败的提交不得进入上游任何分支，也不得合并到 `main`。

仓库同时开启 merge commit、squash 和 rebase，并启用合并后自动删除主题分支。维护者按 merge commit、squash、rebase 的顺序选择合并方式；GitHub 没有仓库级“首选顺序”开关，因此该顺序由本治理规则和维护操作共同保证。

## 发布标签规则集

对 `v*` 标签禁止更新和删除，并要求创建标签的提交已位于受保护的 `main`。如果以后发布可下载构建产物，应同时生成 SHA-256 清单、SPDX 软件物料清单和构建证明；当前项目不发布远端部署产物。

## 真实验收

1. 从独立分支提交一个无害中文文档变更并创建拉取请求。
2. 确认四个必需状态都落在拉取请求最新提交上，而不是临时合并提交上。
3. 确认修改受保护治理文件时“治理规则未被绕过”失败；添加“治理变更”标签和治理变更说明后才允许重新评估。
4. 确认未更新文件清单、降低覆盖率、加入真实数据形态或外部连接时相应检查失败。
5. 不使用管理员绕过完成一次 merge commit 合并；仅在该方式不可用时依次验证 squash、rebase，并确认 `main` 上的推送复验再次成功。
