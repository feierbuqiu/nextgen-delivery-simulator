# 架构说明

应用是独立实现的单页前端，采用清晰的领域边界和本地适配层：

- `src/domain`：业务状态、校验规则、命令和纯函数。
- `src/features`：按工作区组织的 React 交互界面。
- `src/infrastructure`：浏览器本地持久化及仓储适配。
- `src/app`：入口、导航和仅限本机运行的边界控制。
- `src/ui`：跨工作区复用的无业务含义界面组件。
- `src/test`：自动化测试使用的现场授权与浏览器环境辅助设施。

运行时不依赖后端服务。浏览器数据通过仓储接口与领域逻辑隔离，测试可以使用内存或模拟存储实现。构建结果关闭源码映射，并携带禁止索引的响应元数据。
