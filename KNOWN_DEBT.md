# 已知债务与证据边界

本文件只记录公开仓库中仍可复现、尚未关闭的工程债务。能力目录中的“已实现”表示代码与自动化契约存在，不自动等于真实用户路径已经验收。

## 当前优先债务

1. 公开树没有浏览器端到端视觉基线。`npm run check:dev` 只验证受支持 Node.js 下的回环开发入口、React 转换和安全响应头；核心寄递链路由领域、组件和备份往返测试共同覆盖，但仍需独立的人工浏览器验收结论。
2. `src/styles.css` 仍是体量较大的集中样式文件，已接近仓库预算。新增界面前应先拆分稳定的功能样式边界，不能靠继续提高预算掩盖增长。
3. 覆盖率门槛与当前覆盖率之间的余量有限。新增分支必须同时增加对应异常路径测试，不能降低阈值或排除业务文件来换取通过。
4. 三个 IndexedDB 默认名称仍沿用早期本机命名。改名会影响既有浏览器数据，必须先设计可验证、可回滚且不会误导入其他本机应用数据的迁移，再实施命名隔离。
5. GitHub 远端规则需要定期复核，目前仓库内检查只能证明声明和工作流没有漂移，不能单独证明远端规则集、代码扫描与安全设置仍处于启用状态。

## 明确边界

- 运输班交割只模拟本机状态推进、派车单采集和主管现场授权，不连接现实运输、调度或生产接口。
- 自动化不得代替员工签到、主管授权、机构核准或领导审批。
- 任何能力只有在证据路径真实存在、自动化可执行并完成独立用户路径复核后，才能从“已实现”升级为“已验收”。
